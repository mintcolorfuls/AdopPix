@model PostViewModel
    <div>
        <img src="@Url.Content($"https://adoppix.s3.ap-southeast-1.amazonaws.com/{@Model.ImageName}")" style="width:100px; height: 100px;" id="blah" />
        <h2>@Model.UserId</h2>
        <h3>@Model.Title</h3>
        <h4>@Model.Description</h4>
    </div>